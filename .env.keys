# Replyline — secret names for DevVault / load-secrets (no values; see docs/secrets-management.md).
# LLM access is usually configured in-app; optional env keys below are for scripts such as runtime-preflight.

DEEPGRAM_API_KEY
OPENROUTER_API_KEY
LLM_API_KEY
