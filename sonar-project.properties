sonar.projectKey=iurii-izman_replyline
sonar.organization=iurii-izman

# Keep analysis focused on real source artifacts.
sonar.exclusions=**/node_modules/**,**/dist/**,**/coverage/**,**/reports/**,**/src-tauri/target/**

# CPD exclusions for mirrored locale dictionaries and non-product scaffolding/tests.
sonar.cpd.exclusions=src/app/locale.ts,src/app/**/*.test.ts,src/app/**/*.test.tsx,scripts/*handoff-bundle.ps1,scripts/parse-pipeline-latency*.mjs
