name: Windows Packaging Manual (Non-Publishing)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to package (branch, tag, or commit SHA)"
        required: false
        default: "main"
        type: string

permissions:
  contents: read

jobs:
  package-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5
        with:
          version: 9.15.9

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Windows artifacts (no publish)
        run: pnpm tauri build

      - name: Upload Windows bundle artifacts
        uses: actions/upload-artifact@v7
        with:
          name: replyline-windows-bundle-${{ github.run_id }}
          retention-days: 14
          path: |
            src-tauri/target/release/bundle/**
            src-tauri/target/release/*.exe
